@model myBug.Models.Bug
@using myBug.Models

@{
    ViewData["Title"] = "Adicionar Comentario";
}
<h2>@ViewData["Title"]</h2>
<h3>@ViewData["Message"]</h3>

<form asp-action="Comentar" asp-controller="Comentario" method="post">

    @Html.DropDownList("Titulo", (IEnumerable<SelectListItem>)ViewBag.Comentario, String.Empty, new { @class = "extra-class" })
    @Html.ValidationMessageFor(model => model.Id)
    <input type="text" name="Desc_Comentario" /> <br /> <br />

    <input type="submit" value="Comentar" /> <br /> <br />
    <!--  Html.DropDownListFor(model => model.Id, new SelectList(ViewBag.Comentario,  "Id" , "Titulo"), "--Select Bug--")-->
    <!--  Html.DropDownList("Titulo", (IEnumerable<SelectListItem>)ViewBag.Con, String.Empty, new { class = "extra-class" }) -->
    <!--  ViewBag.Con = new SelectList(_banco.Bugs, "Id", "Titulo");-->

</form>



